# Vehicle Edge Runtime Configuration

# Server Configuration
PORT=3002
LOG_LEVEL=info

# Runtime Name (optional)
# If set, the runtime will register as: Edge-Runtime-<hash>-<RUNTIME_NAME>
# If empty, the runtime will register as: Edge-Runtime-<hash>
RUNTIME_NAME=

# Kit Manager Connection
KIT_MANAGER_URL=ws://localhost:3090

# Data Storage
DATA_PATH=./data

# Docker Configuration
DOCKER_HOST=unix:///var/run/docker.sock

# Application Limits
MAX_CONCURRENT_APPS=10
DEFAULT_MEMORY_LIMIT=524288000
DEFAULT_CPU_LIMIT=50000

# Console Configuration
MAX_BUFFER_SIZE=10000
MAX_BUFFER_LINES=1000

# Vehicle Integration (for future use)
KUKSA_DATABROKER_URL=http://localhost:8090
VSS_CONFIG_PATH=./configs/vss.json

# Security
ENABLE_AUTH=false
JWT_SECRET=vehicle-edge-runtime-secret