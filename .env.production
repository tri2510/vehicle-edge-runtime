# Vehicle Edge Runtime - Production Environment Configuration
# Smart defaults for edge deployment

# Runtime Configuration
NODE_ENV=production
PORT=3002
LOG_LEVEL=info

# Runtime Name (optional)
# If set, the runtime will register as: Edge-Runtime-<hash>-<RUNTIME_NAME>
# Example: RUNTIME_NAME=production → Edge-Runtime-a1b2c3-production
RUNTIME_NAME=

# Smart Default Connections
# Kit-Manager: Always-online digital auto service
KIT_MANAGER_URL=wss://kit.digitalauto.tech

# Kuksa Databroker: Local by default, customizable
KUKSA_HOST=localhost
KUKSA_PORT=55555

# Data and Resources
DATA_PATH=/app/data
MAX_CONCURRENT_APPS=10
DEFAULT_MEMORY_LIMIT=524288000  # 512MB per app
DEFAULT_CPU_LIMIT=50000          # 50% CPU per app

# Feature Toggles
SKIP_KIT_MANAGER=false
SKIP_KUKSA=true  # Skip Kuksa by default (use kuksa profile for local Kuksa)